URL=http://localhost:8080

# Optional: point content loading at an Obsidian vault.
# Relative paths resolve from the repository root.
# Also used by `npm run sync:content` and `npm run sync:content:published`.
OBSIDIAN_VAULT_PATH=
OBSIDIAN_POSTS_DIR=Posts
OBSIDIAN_PROJECTS_DIR=Projects
